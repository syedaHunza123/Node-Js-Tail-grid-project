const signButton = document.getElementById('open-modal');

        // Redirect to the form page when the button is clicked
        signButton.addEventListener('click', () => {
            window.location.href = 'signup.html'; // Redirect to form.html
        });